/**
 * Re-export of the centralized URL normalization module.
 * Canonical implementation lives in packages/shared (used by web, worker,
 * and scanner alike).
 */
export * from "@fluxen/shared/url";
